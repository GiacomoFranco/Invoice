/**
 * Type representing the possible states of an invoice.
 */
export type TStatus = 'Paid' | 'Unpaid' | 'Overdue';

/**
 * Type representing the slug of the possible states of an invoice.
 */
export type TStatusSlug = 'paid' | 'unpaid' | 'overdue';
